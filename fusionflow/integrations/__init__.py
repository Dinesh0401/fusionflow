"""Optional integrations for FusionFlow (MLflow, W&B, Jupyter, ...).

Each integration is gated behind an extra (e.g., ``pip install fusionflow[mlflow]``).
Import errors are surfaced clearly with installation hints.
"""
